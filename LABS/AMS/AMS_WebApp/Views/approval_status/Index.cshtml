@model IEnumerable<ASM_UI.Models.ms_approval_status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.approval_status_code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.approval_status_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.approval_status_code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.approval_status_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.approval_status_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.approval_status_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.approval_status_id })
        </td>
    </tr>
}

</table>
