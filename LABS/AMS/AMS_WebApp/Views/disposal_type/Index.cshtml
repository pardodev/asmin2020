@model IEnumerable<ASM_UI.Models.ms_disposal_type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.disposal_type_code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.disposal_type_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.disposal_by_dept_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.disposal_type_code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.disposal_type_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.disposal_by_dept_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.disposal_type_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.disposal_type_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.disposal_type_id })
        </td>
    </tr>
}

</table>
